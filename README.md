# Create a K-means clustering algorithm to group customers of a retail store based on their purchase history.
This K-means clustering model performs market basket analysis.It tries to segregte the customers of a retail store based on some basic data about the customers like Customer ID, age, gender, annual income and spending score.The dataset consists of 5 columns and 201 rows.
